```@meta
CurrentModule = TermPlot
```

# API Reference

This page is generated primarily from the public docstrings in `TermPlot.jl`.
The same text is available in the Julia REPL via `?Figure`, `?panel!`,
`?line!`, and the other exported names.

Use [Concepts](concepts.md) for the `Figure`/`Panel` mental model and
[Rendering Output](rendering.md) for plain-text and SVG integration details.

## Core Types And Constructors

```@autodocs
Modules = [TermPlot]
Pages = ["termplot/core.jl"]
Private = false
Order = [:type, :function]
```

## Plotting, Axes, And Plain-Text Rendering

```@autodocs
Modules = [TermPlot]
Pages = ["termplot/api.jl"]
Private = false
Order = [:function]
```

## SVG Output

```@autodocs
Modules = [TermPlot]
Pages = ["termplot/svg.jl"]
Private = false
Order = [:function]
```
